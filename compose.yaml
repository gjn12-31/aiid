services:
  game:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    env_file: .env.local
    environment:
      DATABASE_PATH: /app/data/mystery.sqlite
    volumes:
      - mystery-data:/app/data
    restart: unless-stopped
volumes:
  mystery-data:
